interface HeroesPlayedInterface{
  hero_id?: number,
  localized_name?: string,
  last_played?: number,
  games?: number,
  win?: number,
  with_games?: number,
  with_win?: number,
  against_games?: number,
  against_wins?: number,
  img?: string,
  roles?: Array<string>
}
